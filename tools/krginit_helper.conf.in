# Filesystem root of the local instance of the Kerrighed container
#KRG_ROOT=/

# Userspace helper completing the setup of the local Kerrighed container
# and responsible for calling krg_node_ready(2)
# exec()'ed by krginit_helper
#KRGINIT=/usr/local/bin/krginit

# krginit_helper gives the following arguments to $KRGINIT.
# With krginit, -- is followed by an optional "final" local init process in the
# Kerrighed container.
# The default arguments tell krginit to run the scripts in krginit_helper-post.d
# before either exec()'ing sshd on port 2222 (boot node), or exec()'ing krginit
# as final local init process (added nodes).
KRGINIT_ARGS='-- /bin/sh -c "NODES=\`@sbindir@/krgadm nodes | /bin/grep -c online\`; /bin/run-parts -- @sysconfdir@/kerrighed/krginit_helper-post.d; [ \$NODES = 1 ] && exec /usr/sbin/sshd -D -p 2222; exec @sbindir@/krginit"'
